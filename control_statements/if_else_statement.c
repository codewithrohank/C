#include <stdio.h>

int main()
{
    int num1 = 100, num2 = 20;
    
    if (num1 < num2)
    {
        printf("%d is smaller than %d\n", num1, num2);
    } 
    else
    {
        printf("%d is bigger than %d\n", num1, num2);
    }

    return 0;
}

